import { stringOfFormResponseReceivedEmail } from './index';

describe('Import and Export Test', () => {
  it('check if there is a correct function - stringOfFormResponseReceivedEmail', () => {
    expect(typeof stringOfFormResponseReceivedEmail).toBe('function');
  });
});
