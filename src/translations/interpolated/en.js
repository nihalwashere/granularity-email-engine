import { updateOriginalStringsArray } from '../interpolatedStrings';
import originalStrings from '../original/en.json';

updateOriginalStringsArray(originalStrings);

export const en = originalStrings;
